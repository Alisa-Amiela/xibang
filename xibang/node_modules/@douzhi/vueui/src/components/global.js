Vue.component("item", {
  render(createElement) {
    let componentName = this.$parent.$options.name;
    switch (componentName) {
      case "dz-breadcrumb":
        return createElement("dz-breadcrumb-item", null, [this.$slots.default]);
    }
  }
});
